<appendix id="supportedgc">
<title>Display bus access</title>
<sect1>
<title><filename>/dev/i2c-*</filename> userspace I2C support</title>
<para>DDCcontrol accesses monitor DDC/CI busses only through
<filename>/dev/i2c-*</filename> devices provided by the operating system. This
uses the same kernel userspace I2C path as tools such as
<filename>ddcutil</filename> for directly connected displays.</para>
<para>Legacy direct PCI memory access and AMD ADL support have been removed.
Use the kernel graphics driver and <filename>i2c-dev</filename> support to
expose monitor DDC busses as <filename>/dev/i2c-*</filename> devices.</para>
<para>USB-connected DDC/CI displays are not supported yet.</para>
<sect2>
<title>Using NVIDIA proprietary drivers</title>
<para>The NVIDIA proprietary driver exposes the I2C bus through the standard
<filename>/dev/i2c-*</filename> interface.  However, on modern drivers
(particularly with monitors connected via DisplayPort or HDMI) the driver may
fail to service I2C requests, causing DDC/CI communication to fail.</para>
<para>The fix is to force the driver to use software I2C by adding an Xorg
configuration snippet.  DDCcontrol ships a ready-made file at
<filename>/usr/share/ddccontrol/90-nvidia-i2c.conf</filename>.
Copy it to the Xorg configuration directory and restart your X session:</para>
<screen format="linespecific">
# cp /usr/share/ddccontrol/90-nvidia-i2c.conf /etc/X11/xorg.conf.d/
</screen>
<para>The file sets the following NVIDIA registry keys:</para>
<screen format="linespecific">
Option "RegistryDwords" "RMUseSwI2c=0x01; RMI2cSpeed=100"
</screen>
<para>See the
<ulink url="https://forums.developer.nvidia.com/t/gddccontrol-issues-with-nvidia-drivers-i2c-monitor-display-ddc-dp-hdmi-failing/30427">NVIDIA developer forum</ulink>
for more background on this issue.</para>
</sect2>
</sect1>
</appendix>
