# Install example configuration files into $(pkgdatadir) so they are
# discoverable by users and packagers.  They are not installed directly into
# /etc/X11/xorg.conf.d/ to avoid silently changing X11 behavior on install.

pkgdata_DATA = 90-nvidia-i2c.conf

EXTRA_DIST = $(pkgdata_DATA)
