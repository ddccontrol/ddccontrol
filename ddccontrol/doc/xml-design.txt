Monitor database design

The directory db contains the following files:
options.xml
monitor/%monitor_id%.xml

options.xml
-----------

Controls (for example "brightness"...) are contained in groups
(for example "color settings"). Groups allow the GUI to separate the
options in tabs.

Controls IDs are usually like Control Name, but abbreviated.

Controls with type set to "value" are controls where you can define
a value (for example brightness), and those with type set to "command"
are controls that do something when the value is set to something >0
(for example degauss), "list" indicates a list of possible value.

File format:

<?xml version="1.0"?>
<options>
  <group name="%groupname%">
    <control id="%control_id%" type="value" name="%control_name%"/>
    <control id="%control_id%" type="[list|command]" name="%control_name%">
      <value id="%value_id%" name="%value_name%"/>
      <value...>
    </control>
    <control ...>
  </group>
  <group ...>
<options>

monitor/%monitor_id%.xml
------------------------

List of controls related to this monitor.
Controls IDs are defined in options.xml, if it is a list or a command, 
supported values must be defined.

The init parameter in monitor define the type of the initialisation:
DDC/CI standard ("standard"), or Samsung ("samsung"). 

You can set a parameter delay to set the time in milliseconds to wait
after changing the value of this control. If you don't, a default value
is used.

File format:

<?xml version="1.0"?>
<monitor name="%full_monitor_name%" init="[standard|samsung]">
  <controls>
    <control id="%control_id%" address="%hex_address%" delay="%wait_time%"/> <!--- value -->
    <control id="%control_id%" address="%hex_address%" delay="%wait_time%"> <!--- command -->
      <value id="activate" value="%hex_value%"/>
    </control>
    <control id="%control_id%" address="%hex_address%" delay="%wait_time%"> <!--- list -->
      <value id="%value_id%" value="%hex_value%"/>
      <value...>
    </control>
    <control...>
  </controls>
</monitor>

If the monitor is exactly like another one, you can define it
as shown:

<?xml version="1.0"?>
<monitor include="%monitor_id%" name="%full_monitor_name%"/>
