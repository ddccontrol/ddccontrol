--- a/debian/control
+++ b/debian/control
@@ -3,6 +3,11 @@
 Maintainer: Barak A. Pearlmutter <bap@debian.org>
 Homepage: https://github.com/ddccontrol/ddccontrol
 Build-Depends: debhelper-compat (= 13),
+ cargo,
+ rustc,
+ librust-encoding-rs-dev,
+ librust-libc-dev,
+ librust-roxmltree-dev,
  libpci-dev,
  intltool,
  libxml2-dev,
@@ -46,7 +51,7 @@
 Architecture: any
 Multi-Arch: same
 Pre-Depends: ${misc:Pre-Depends}
-Depends: libddccontrol0 (= ${binary:Version}),
+Depends: libddccontrol1 (= ${binary:Version}),
  ${misc:Depends}
 Description: development files for ddccontrol
  DDCcontrol is a tool used to control monitor parameters, like brightness
@@ -55,9 +60,11 @@
  .
  This package provides the development files.
 
-Package: libddccontrol0
+Package: libddccontrol1
+Breaks: libddccontrol0 (<< 3.0.0)
+Replaces: libddccontrol0 (<< 3.0.0)
 Section: libs
-Architecture: i386 amd64 ia64 alpha arm armel armhf arm64 loong64
+Architecture: any
 Multi-Arch: same
 Pre-Depends: ${misc:Pre-Depends}
 Depends: ${shlibs:Depends},
--- a/debian/ddccontrol.install
+++ b/debian/ddccontrol.install
@@ -8,3 +8,4 @@
 usr/share/dbus-1/system.d/*.conf
 
 usr/share/doc/ddccontrol/*.html usr/share/doc/ddccontrol/html/
+usr/share/ddccontrol/90-nvidia-i2c.conf
--- a/debian/rules
+++ b/debian/rules
@@ -17,6 +17,7 @@
 	dh_auto_install -- modulesdir=$(modulesdir) dbusconfdir=/usr/share/dbus-1/system.d
 
 override_dh_auto_test:
+	dh_auto_test
 
 execute_after_dh_install:
 	@echo 'Strip wrong #include "config.h" from ddcci.h (bug#518083)'
--- a/debian/ddccontrol.docs
+++ b/debian/ddccontrol.docs
@@ -1,3 +1,2 @@
-NEWS
+AUTHORS
 README.md
-TODO
--- a/debian/libddccontrol0.install
+++ b/debian/libddccontrol0.install
@@ -1,3 +1,4 @@
 ${env:modulesdir}/ddccontrol-i2c-dev.conf
-usr/lib/*/lib*.so.*
+usr/lib/*/libddccontrol.so.1*
+usr/lib/*/libddccontrol_dbus_client.so.0*
 usr/lib/*/ddccontrol/*
